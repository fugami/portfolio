/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts

# local env
.env
.env.local
.env*.local

# local-only uploads (used when Supabase storage is not configured)
/public/uploads/*
!/public/uploads/.gitkeep

# local working store — seeded from data/seed.json on first run (seed is canonical)
/data/store.json

# macOS
.DS_Store
